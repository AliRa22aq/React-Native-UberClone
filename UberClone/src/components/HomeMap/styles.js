import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        // height: 300,
        // backgroundColor: "#e0abff",
        // justifyContent: 'center',
        // alignItems: "center"
    }
})

export default styles;